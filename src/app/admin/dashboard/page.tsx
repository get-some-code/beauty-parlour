import AdminPanel from "@/components/AdminPanel";

export const metadata = {
  title: "Admin Dashboard | Habibs Hair & Beauty",
  robots: { index: false, follow: false },
};

export default function AdminPage() {
  return <AdminPanel />;
}